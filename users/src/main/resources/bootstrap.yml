spring:
  application:
    name: user-service
eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:11001/eureka/
  instance:
    hostname: localhost